import { NextResponse } from "next/server";
import { randomUUID } from "node:crypto";
import { requestSession, assertOrigin, AppError } from "@/lib/platform/auth";
import { query } from "@/lib/platform/db";
import { apiError, readJson } from "@/lib/platform/http";
export async function POST(request: Request) { try { assertOrigin(request); const user=await requestSession(request); if(!user) throw new AppError("Please sign in.",401); const e=(await query<{id:string}>("SELECT id FROM wl.employees WHERE user_id=$1 AND active=true",[user.user_id]))[0]; if(!e) throw new AppError("No employee profile is linked to this account.",403); const { action }=await readJson(request) as {action:string}; const active=(await query<{id:string;break_started_at:string|null}>("SELECT id,break_started_at FROM wl.time_entries WHERE employee_id=$1 AND clock_out IS NULL",[e.id]))[0]; if(action==='in'){if(active) throw new AppError("You are already clocked in."); await query("INSERT INTO wl.time_entries(id,employee_id,clock_in) VALUES($1,$2,now())",[randomUUID(),e.id]);} else if(action==='out'){if(!active) throw new AppError("Clock in before clocking out."); if(active.break_started_at) throw new AppError("End your break before clocking out."); await query("UPDATE wl.time_entries SET clock_out=now() WHERE id=$1",[active.id]);} else throw new AppError("Unsupported clock action."); return NextResponse.json({ok:true}); } catch(e){return apiError(e);} }
